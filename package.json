{
  "name": "neurotrace-eeg-digital-twin",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive EEG focal-seizure replay synchronized with a 24-region anatomical brain digital twin.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build-site.mjs",
    "check": "node --check eeg-demo.js && node --check scripts/build-site.mjs && node --check scripts/dev-server.mjs && python3 -m py_compile scripts/build_brain_glb.py",
    "verify": "npm run check && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
