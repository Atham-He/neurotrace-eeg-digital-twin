{
  "name": "neuroscope-eeg-dashboard",
  "version": "0.2.0",
  "description": "Multi-device EEG replay, signal-quality gating and anatomical seizure visualization demo.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build-site.mjs",
    "check": "node --check app.js && node --check brain-renderer.js && node --check device-profiles.js && node --check validation-framework.js && node --check cortex-client.js && node --check generic-device-client.js && node --check scripts/dev-server.mjs && node --check scripts/build-site.mjs",
    "verify": "npm run check && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
